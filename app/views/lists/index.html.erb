<h1>Mes Listes</h1>

<% if flash[:success] %>
  <div class="alert alert-success"><%= flash[:success] %></div>
<% end %>

<% if flash[:error] %>
  <div class="alert alert-danger"><%= flash[:error] %></div>
<% end %>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
      <%= button_to "Supprimer", list_path(list), method: :delete, data: { confirm: "Êtes-vous sûr de vouloir supprimer cette liste et tous ses films?" }, class: "btn btn-danger btn-sm" %>
    </li>
  <% end %>
</ul>

<%= link_to "Créer une nouvelle liste", new_list_path, class: "btn btn-primary" %>
